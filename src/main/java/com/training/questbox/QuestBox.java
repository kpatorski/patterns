package com.training.questbox;

import com.training.hero.Hero;
import com.training.questbox.equipment.Equipment;

class QuestBox {

    Equipment rewardForHero(Hero hero) {
        //TODO: Add logic for creating Hero's equipment, QuestBox should know nothing about existence of Magic and Ordinary items
        return new Equipment();
                //.add(helmet)
                //.add(armor)
                //.add(legs);
    }
}
